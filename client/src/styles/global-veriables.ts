import { createGlobalStyle } from 'styled-components';

export const GlobalVariables = createGlobalStyle`
  :root {
    --min-max-spacing: 4px;
    --min-spacing: 8px;
    --md-spacing: 16px;
    --md-max-spacing: 18px;
    --big-spacing: 36px;
    --big-max-spacing: 46px;
    --min-radius: 5px;
  }
`;
